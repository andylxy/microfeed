# 权限树 + 级联：设计问题清单（待逐条勾）

> **场景**：`/admin/rbac/` 的角色权限从「按 `module:resource` 平铺的网格」改为「树形 + 级联」。
>
> **状态**：**已实现并部署**（2026-09-23 14:19，`.microfeed/deploy-menu-subnav.log` → `Deployed and verified`）。  
> 所以这份清单是**设计复盘**——确认已经做掉的选择，补齐还没定的分支。
>
> **勾法**：`[x]` = 同意右侧答案；不同意就在 `↩ 改：` 后面写你的版本；留空 = 待你定。

## 图例

- 🟢 **已定** —— 实现时已经做的选择（确认即可；要推翻请说明）
- ❓ **第 1 轮** —— 现在就能答
- ⏳ **第 2 轮** —— 依赖前项答案，先别急着答

---

## A. 树的结构

- [ ] **A1** 🟢 树建在哪个维度？  
  → 权限模块（`content` / `system` / `api`），**不是**菜单结构  
  ↩ 改：菜单结构
- [ ] **A2** 🟢 树有几层？  
  → 固定 3 层：`module → resource → action`（权限码本身就是这个形状）  
  ↩ 改：按实际
- [ ] **A3** ❓ 内部节点（模块 / 资源）是**真实权限码**还是**纯 UI 分组**？  
  → 纯 UI 分组；只有叶子（`content:book:read`）才是 `ext_permissions` 里的码  
  ↩ 改：**真实权限码**
- [ ] **A4** 🟢 不满足 `module:resource:action` 的码（如 `*`）怎么办？  
  → 跳过，不进树  
  ↩ 改：
- [ ] **A5** 🟢 每层怎么排序？  
  → 字母序（`localeCompare`）  
  ↩ 改：
- [ ] **A6** ⏳ 空模块 / 空资源会出现吗？  
  → 取决于 A3：内部节点若是纯分组，"空"即不存在  
  ↩ 改：空"即不存在

## B. 级联语义

- [ ] **B1** ⏳ 勾父节点 = 授予全部叶子（**显式写死叶子码**）还是记**继承关系**？  
  → 待 A3 定；我倾向显式写死叶子码  
  ↩ 改：授予全部叶子（**显式写死叶子码**
- [ ] **B2** ⏳ 以后新增权限码，老角色**自动获得**吗？  
  → B1 的直接后果：显式 ⇒ **不会**（新码要手动勾）  
  ↩ 改：新码要手动勾
- [ ] **B3** ⏳ 取消勾父节点 = 撤销它的全部叶子？  
  → 是 撤销它的全部叶子  
  ↩ 改：
- [ ] **B4** ⏳ 父节点自身能不能"已授予"（即允许"只给模块、不给具体叶子"）？  
  → 不能（模块不是权限）  

- [ ] **B5** 🟢 半选（indeterminate）的作用？  
  → 只是显示，不影响保存  
  ↩ 改：
- [ ] **B6** 🟢 保存载荷？  
  → 只发叶子码，沿用现有 `role-permissions` 端点（整量覆盖）  
  ↩ 改：

## C. 特殊角色与通配符

- [ ] **C1** ❓ `*` 通配符要出现在树里吗？  
  → 不出现（勾了它 = 把普通角色变成超管，是提权路径）  
  ↩ 改： 不出现
- [ ] **C2** ❓ `super_admin` 进这个页面时，树是什么状态？  
  → 整树只读（沿用现有 `isWildcardRole`）  
  ↩ 改： 整树选中
- [ ] **C3** ⏳ 一个角色持有 `*` 但不是 `super_admin`（可能吗）？怎么显示？  
  → 待定（服务端拒绝把 `*` 授予普通角色，理论上不会出现）  
  ↩ 改：一个角色不能持有 `*`
- [ ] **C4** ⏳ legacy admin（`auth_user.role === 'admin'`）在树里怎么体现？  
  → 待定  整树选中  
  ↩ 改：
- [ ] **C5** ⏳ `readonly` 角色的默认勾选怎么定？  
  → 待定  选中  
  ↩ 改：

## D. 标签与呈现

- [ ] **D1** ❓ 模块名 / 资源名从哪来？  
  → i18n（`rbac.module.*` + `rbac.group.<module>_<resource>`）  
  ↩ 改：按实际获取
- [ ] **D2** 🟢 标签缺失时怎么回退？  
  → 显示原始码（如 `content:audit`）  
  ↩ 改：
- [ ] **D3** ❓ 视觉形态？  
  → 嵌套 checkbox（当前实现）  
  ↩ 改：checkbox（当前实现）
- [ ] **D4** ❓ 要不要保留"平铺网格"作为可切换视图？  
  → 不要  
  ↩ 改：不要
- [ ] **D5** ❓ 要不要加搜索 / 过滤（像设置侧栏那样）？  
  → 暂不要  
  ↩ 改：不要
- [ ] **D6** 🟢 无障碍：半选状态怎么表达？  
  → 原生 checkbox 的 `indeterminate` 已被 AT 读作 `mixed`，无需额外 aria  
  ↩ 改：
- [ ] **D7** 🟢 禁用态？  
  → `saving` 或只读角色时整树 disabled  


## E. 保存与数据

- [ ] **E1** 🟢 保存粒度？  
  → 整角色一次性提交  
  ↩ 改：
- [ ] **E2** 🟢 树会不会改 DB schema？  
  → 不会，纯 UI  
  ↩ 改：
- [ ] **E3** ❓ 勾选变化要不要**审计**？  
  → 暂不要  
  ↩ 改：不要

## F. 与菜单的关系

- [ ] **F1** ❓ 树和菜单（`ext_menu`）要不要**打通**（"菜单看得见 ⇔ 权限有"）？  
  → 不打通（语义不同：菜单是 UI 结构，权限是授权）  
  ↩ 改：不打通
- [ ] **F2** 🟢 有权限但没菜单的码（如 `system:permission:manage`）？  
  → 照常显示在树里  
  ↩ 改：
- [ ] **F3** 🟢 一个菜单绑多个权限时？  
  → 不在树上合并，各码独立  
  ↩ 改：

## G. 文档与测试

- [ ] **G1** ❓ 决策记录写在哪？  
  → 本仓建 `CONTEXT.md`（术语表）+ `docs/adr/`（本仓自己的 ADR）  
  ↩ 改：本仓
- [ ] **G2** ❓ 术语表收哪些词？  
  → 模块 / 资源 / 操作 / 叶子 / 级联 / 半选 / 通配符  
  ↩ 改：确认
- [ ] **G3** ❓ 哪条值得写 ADR？  
  → 1 条：「权限树建在权限模块上（而非菜单）」  
  ↩ 改：确认
- [ ] **G4** 🟢 测试覆盖到哪？  
  → 纯函数 `buildPermissionTree` / `permissionBranchState` 已 5 例；组件渲染 1 例  
  ↩ 改：
- [ ] **G5** ❓ 要不要补**级联交互**测试（勾父 → 子全变、父呈半选）？  
  → 建议补  
  ↩ 改：补

---

## 参考事实（我查的，供你判断）

- 权限码共 **35** 个：`*`(1) + `api:*`(8) + `content:*`(21) + `system:*`(5)；形态一律 `module:resource:action`。
- 资源共 **20** 个：content 10（article / audit / book / category / channel / page / review / settings / site_file / volume）、system 5（api / permission / role / user / webhook）、api 4（content / media / page / site）。
- 实现位置：`src/shared/Rbac.ts`（`buildPermissionTree`、`permissionBranchState`）、`src/components/admin/rbac/RbacApp/index.tsx`。
- 本仓现状：**没有** `CONTEXT.md` / `docs/adr/`；设计文档在 `.scratch/microfeed-rbac/`，ADR 在跨仓 `D:\git\AiCode\simple-rbac-design\`（代码注释里的 `ADR-001/002/010/011` 指向那边）。

## 统计

🟢 已定 **13** 条 ｜ ❓ 第 1 轮 **10** 条 ｜ ⏳ 第 2 轮 **7** 条

---

# Round 2 —— 你的答案推翻了树的结构，先解一个硬矛盾

## 我读到的答案（2026-09-23 15:12）

**推翻我的实现（⇒ 要重做）**：
A1 → **菜单结构** ｜ A2 → 按实际 ｜ A3 → **真实权限码** ｜
C2 → super_admin **整树选中** ｜ C4 → legacy admin **整树选中** ｜ C5 → readonly **选中** ｜
D1 → 按实际获取

**确认（与我的答案一致）**：
A4 A5 A6 B1 B2 B3 B5 B6 C1 C3 D2 D3 D4 D5 D6 E1 E2 E3 F1 F2 F3 G1 G2 G3 G4 G5

**未答**：B4（父节点能否单独授予）、D7（禁用态）

## ⚠️ 硬矛盾：`菜单结构` + `真实权限码` 与现在的表对不上

| 事实 | 现状 |
| --- | --- |
| `ext_menu` | 21 行 = 6 个根（首页 + 5 分组）+ 15 个页面 |
| 分组行 | `permission_code = **NULL**` —— 它绑不了任何权限 |
| 页面行 | 每行只绑 **1** 个码（`books` → `content:book:read`） |
| `ext_permissions` | 35 个码，形态全是 `module:resource:action`；**没有** `content` / `content:book` 这种模块级 / 资源级的码 |
| 一个页面的真实权限 | `books` 实际涉及 **4** 个码（read / create / update / delete），不是 1 个 |

⇒ 「菜单树 + 每个节点都是真实权限码」**目前没有可用的映射**：分组没码，页面又不止一个码。所以要先把 R1–R4 定下来。

## 第 2 轮问题（勾法同上）

- [ ] **R1** 树的层级到底怎么排？（A1 + A2 + A3 的落地）
      → A. 分组 → 页面 → 该页的权限码（3 层）
      → B. 分组 → 页面（页面即勾选单位，点它 = 给该页全部码）
      → C. 分组 → 页面 → 权限码，且**分组与页面各自绑一个真实码**
      ↩ 选：
- [ ] **R2** 分组节点（内容 / 审核 / 站点 / 集成 / 账户）绑什么码？它们现在是 `NULL`。
      → A. 新建 5 个"分组级"权限码（迁移），如 `group:content`
      → B. 分组不绑码，只当标题（勾它 = 勾其下全部）
      → C. 复用某个已有码（但现有码没有模块级，等于要新建）
      ↩ 选：
- [ ] **R3** 一个页面的多个码（`books` 的 read / create / update / delete）在树里怎么呈现？
      → A. 展开成 4 个叶子（当前实现的做法）
      → B. 页面层一个勾，勾上 = 4 个码全给（不展示单个码）
      → C. 页面层 + 叶子层（可展开细调）
      ↩ 选：
- [ ] **R4** "真实权限码"是否意味着要给**模块 / 资源新建权限码行**（迁移 + `ext_permissions` 补行）？
      → 是 / 否，请说明
      ↩ 改：
- [ ] **R5** `super_admin` / legacy admin 的"整树选中"是哪种？
      → A. 全部勾选 **且只读**（不可改）
      → B. 全部勾选 **且可编辑**（但服务端仍拒绝给 super_admin 写 `*`）
      ↩ 选：
- [ ] **R6** `readonly` 的"选中"具体指？
      → A. 只是"它现有授权如实显示为勾选"（自动，无需额外逻辑）
      → B. 建号默认角色时，树里**默认全选** readonly 该有的码
      ↩ 选：
- [ ] **R7** **B4**：父节点自身能不能"已授予"（即允许"只给模块 / 页面、不给具体叶子"）？
      ↩ 改：
- [ ] **R8** **D7**：禁用态——`saving` 或只读角色时整树 disabled？
      ↩ 改：
- [ ] **R9** 确认**重做**：我 14:19 部署的版本按"权限模块"做的，与 `A1 = 菜单结构` 冲突。确认按新口径**重做**这棵树？
      → 是（重做）/ 否（保留现状，只改其它）
      ↩ 改：

---

# Round 3 —— 已确认「按现在菜单分组」

用户于 2026-09-23 15:19 再次明确：`/admin/rbac/` 的权限树**按现在的菜单分组**组织。

已确定的领域语言已写入根目录 `CONTEXT.md`：

- **菜单分组**：内容 / 审核 / 站点 / 集成 / 账户；是导航分类，不叫「权限模块」。
- **权限树**：角色维护中，按照菜单分组组织权限码的管理视图。
- **权限码**：可授予角色的最小授权单位。
- **级联选择**：父节点批量选择后代权限码的编辑动作，不等于权限继承。

## 第 3 轮问题（当前 frontier）

- [ ] **M1** 菜单分组下面的结构？
      → A. **分组 → 页面 → 权限码**（推荐；例如 内容 → 书籍 → read/create/update/delete）
      → B. 分组 → 权限码（不显示页面层）
      → C. 分组 → 页面（不显示单个权限码，页面勾选代表全部）
      ↩ 选：
- [ ] **M2** 分组 / 页面节点是否必须是「真实权限码」？
      → A. **不必须**（推荐）：分组和页面是批量选择节点，只有叶子是真实权限码
      → B. 必须：为 5 个分组 + 15 个页面新建真实权限码（会扩大授权模型）
      ↩ 选：
- [ ] **M3** 菜单页面与多个权限码之间的映射从哪来？（现有 `ext_menu.permission_code` 每页只能绑 1 个码）
      → A. **新增多对多映射**（推荐）：一个页面可映射多个 `ext_permissions`，数据驱动、可测试
      → B. 在前端 / 共享代码硬编码映射
      → C. 按权限码前缀推断（`books` ↔ `content:book:*`；API 等例外另写规则）
      ↩ 选：
- [ ] **M4** 没映射到任何菜单页面的权限码怎么显示？
      → A. **放入「其他权限」分组**（推荐；不静默丢失）+ 测试要求所有码都可见
      → B. 不显示，但测试阻止新增未映射码
      → C. 不显示，也不阻止
      ↩ 选：
- [ ] **M5** 分组 / 页面 checkbox 的持久化语义？
      → A. **只批量改叶子**（推荐）：保存时仍只提交真实权限码；父节点不入库
      → B. 父节点本身也入库（依赖 M2-B 新建权限码）
      ↩ 选：
- [ ] **M6** `super_admin` 的树如何显示？
      → A. **整树选中且只读**（推荐；它持有 `*`，不能从这里拆掉）
      → B. 整树选中且可编辑
      ↩ 选：
- [ ] **M7** legacy admin 如何显示？
      → **事实**：legacy admin 是账号旁路（`auth_user.role='admin'`），不是角色维护页中的角色；因此 `/admin/rbac/` 没有一个可选的「legacy admin 角色」。
      → A. **不在角色权限树体现**（推荐）；只在用户页显示 Better Auth role
      → B. 新建一个可维护的 legacy admin 角色（会改变现有双保险语义）
      ↩ 选：
- [ ] **M8** `readonly` 的「选中」具体含义？
      → A. **如实勾选它当前拥有的权限码**（推荐；不是全选）
      → B. 只读角色整树全选（这与角色名语义冲突）
      → C. 选中 readonly 角色时整树禁用，但只勾它当前拥有的码
      ↩ 选：
- [ ] **M9** 新增权限码后的旧角色？
      → **不会自动获得**；新码进入映射和树后，由管理员手动勾选（与上一轮 B2 一致）
      ↩ 改：
- [ ] **M10** 树默认展开状态？
      → A. **分组展开、页面展开**（推荐；35 个码规模可控，最方便了解结构）
      → B. 分组展开、页面折叠
      → C. 全部折叠
      ↩ 选：
- [ ] **M11** 是否补交互测试？
      → **补**：分组全选 / 页面全选 / 叶子切换 / 半选回填 / 保存只含叶子（与 G5 一致）
      ↩ 改：
- [ ] **M12** 确认重做并替换生产中的「按权限模块」版本？
      → 是 / 否
      ↩ 选：

---

# Round 3 结论（2026-09-23 15:41）—— 已按推荐落地

用户确认：**全部按上一轮我的推荐**（M1–M12 均取 A），并要求写 ADR + 改实现。

- **ADR**：`.scratch/microfeed-rbac/adr/0001-permission-tree-follows-menu.md`
- **术语表**：根目录 `CONTEXT.md`（账号 / 角色 / 权限码 / 菜单分组 / 权限树 / 级联选择 / 半选 / 通配权限）
- **实现**：
  - 迁移 `0052_ext_menu_permissions.sql`：菜单页面 ↔ 权限码多对多（34 条种子；`*` 除外全有归属）
  - `src/shared/Rbac.ts`：`buildPermissionTree` / `togglePermissionBranch` / `permissionBranchState`、
    `RBAC_OTHER_GROUP` / `RBAC_OTHER_PAGE`；`RbacBoard` 新增 `groups`
  - `readRbacBoard`：读 `ext_menu` + `ext_menu_permissions` + 目录，组装树
  - `RbacApp`：渲染 菜单分组 → 菜单页面 → 权限码，父级级联 + 半选
  - i18n：`rbac.otherPermissions` / `rbac.uncategorised`；移除已无用的 `rbac.module.*` / `rbac.group.*`
  - `manage-cli/lib/snapshot.ts`：`ext_menu_permissions` 登记进 durable
- **门禁**：typecheck 0/676、unit 973 passed（+1 skipped）、worker 276 passed、i18n 2001/2001
- **未提交、未部署**
