# 04 — 侧栏按数据渲染（含图标）

**What to build:** 后台侧栏（桌面版与移动端）改成渲染 03 算出来的菜单，图标不再由代码里的硬编码映射决定，而是按菜单数据里的图标名查表，查不到时用兜底图标。客户端依旧只拿到"该显示什么"，拿不到权限集本身。

**Blocked by:** 03（原写「03、05」。实际执行改为 **04 先于 05**：05 要删掉的「菜单码 → 路径」清单，正是 04 接线前仍在使用的那份，先做 05 会弄坏构建。代价只是 04 新增的几行代码多改一次名字。）

**Status:** done

- [ ] 桌面侧栏与移动导航都改为渲染服务端算出的菜单
- [ ] 图标按数据里的图标名查表，缺图标时用兜底图标，不报错、不留空位
- [ ] 当前项高亮与置灰行为与现在一致
- [ ] 客户端数据里不包含权限集合（只含已过滤后的结果）
- [ ] 首屏无闪烁（过滤在服务端完成，不是渲染后再隐藏）
