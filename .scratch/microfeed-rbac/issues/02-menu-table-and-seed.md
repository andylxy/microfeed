# 02 — 菜单表与初始数据

**What to build:** 把后台菜单变成数据。新增一张菜单表，并把现有的 16 个菜单项作为初始数据种进去：每项带稳定菜单码、后台相对路径、i18n 键、图标名、排序，以及**一个可选权限码**——除首页（公共项）外，每一项都绑定 01 里准备的权限点。这一票只交付"数据已就位"，还不改变界面。

**Blocked by:** 01（播种的权限码要先存在）

**Status:** done

- [ ] 菜单表含：菜单码（唯一）、父级码（预留）、后台相对路径片段、i18n 键、图标名、权限码（可空）、排序、是否显示
- [ ] 播种 16 项；首页权限码为空（对任何登录账号可见），其余 15 项各绑一个权限码
- [ ] 索引覆盖父级码与权限码；插入幂等
- [ ] 新表已登记进快照分类（durable），否则快照校验会红
- [ ] 每个菜单码在表里都有对应行（新增断言守住）
