# 05 — 命名统一为「菜单」（宽口径改名，一次性完成）

**What to build:** 把这套概念的名字统一成"菜单 / menu"。现在同一个东西有两套名字：表与领域术语叫 menu，而代码常量、类型、i18n 键叫 nav。统一后：常量改为菜单码常量、类型改为菜单项、16 个 i18n 键由 `nav.item.*` 迁到 `menu.item.*`，同时删掉代码里那份"菜单码 → 路径"的清单（路径已在表里，只留菜单码常量）。侧栏外壳自己的键（不是菜单项的那些）保持不动。

> 这是一次宽口径机械改名（波及约 40 个页面与 32 个 i18n 键），无法切成纵向小片，故作为单独一票一次性改完，门禁在本票末尾统一跑绿。

**Blocked by:** 04（原写「None」，实际改为排在 04 之后 —— 见票据 04 的说明。另：迁移 0041 从未在开发环境之外执行过，因此 i18n 键的迁移直接改在 0041 里，未新增 0042。）

**Status:** done

- [ ] 常量、类型统一为 menu 命名
- [ ] 16 个 i18n 键迁移到 `menu.item.*`（中英文各 16 个），i18n 键一致性校验通过
- [ ] i18n 校验脚本里针对 nav 标签的那段同步改为 menu
- [ ] 删除代码里"菜单码 → 路径"的清单，路径只在表里
- [ ] 侧栏外壳自身的键（导航区标题、公开访问、新建等）**不迁**
- [ ] 门禁：类型检查、i18n 校验、lint、单测全绿
