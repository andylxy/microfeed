# 01 — 菜单可用的权限码与 editor 授权

**What to build:** 让后台菜单有可绑定的权限点，并让「编辑」角色拿到内容域授权。完成这一票后：权限目录新增 6 个码（频道管理、审核管理、审计查看、页面管理、站点文件管理、API 管理），editor 的授权从 11 个增加到 16 个——补上章节的读/建/改（它此前连章节都编辑不了）以及页面与站点文件。editor 仍然拿不到删除章节，也拿不到审核、审计、频道、API 这几项治理类权限。

**Blocked by:** None — can start immediately.

**Status:** done

- [ ] 权限目录新增 6 个码：频道管理、审核管理、审计查看、页面管理、站点文件管理、API 管理（目录总数 29 → 35）
- [ ] editor 补 5 条授权：章节读/建/改、页面管理、站点文件管理（授权数 11 → 16）
- [ ] editor **不含** 章节删除，也不含审核/审计/频道/API 四项
- [ ] SQL 侧与代码侧目录逐项一致（既有的目录同步断言仍绿），editor 授权条数断言同步为 16
- [ ] 迁移幂等（`INSERT OR IGNORE`），与既有权限播种写法一致
