# 2026-09-15

## 工单 01 完成并提交：数据底座与留痕引擎（novel-cms）
- 实现 microfeed novel-cms 工单 01（`.scratch/novel-cms/issues/01-data-foundation-and-audit-engine.md`）。
- 迁移 `migrations/0023_ext_novel.sql`：3 张 `ext_*` 命名空间表（ext_category / ext_content_audit / ext_content_report）+ 加性列 `items.review_status` / `channels.genre`。**移除 ext_category.updated_at**（code-review 判定 scope creep；spec 仅列 created_at）。与上游零冲突。
- 引擎 `src/server/feed/extContentAudit.ts`：`computeDiff`(字段级 add/update/remove，递归 _microfeed) / `shouldCheckpoint`(每 K 次) / `recordAudit`(检查点行必须带 checkpointData 全量快照，否则抛错，落实 ADR-0003) / `rebuildFromCheckpoint`(= applyDiff 重放)。全 DB 访问走最小 `AuditDb` 端口（D1 结构可赋值，便于单测）。
- 单测：`tests/unit/ext-novel-migration.test.ts`（跑完整上游链 0001~0022 + 0023 验证无命名冲突；断言仅 3 张 ext_ 表、ext_category 无 updated_at、加性列在）、`tests/unit/server/extContentAudit.test.ts`（diff / 检查点周期 / 嵌套字段移除重建 / 端到端 K 次恰一行 is_checkpoint=1 且带全量快照 / 检查点无快照抛错）。
- 验证：本沙箱 vitest worker 池无法初始化（环境缺陷非代码），改用 esbuild 转译真实模块 + node:sqlite 内存库做等价运行时验证（44 断言全过）+ 全链迁移验证；`tsc --noEmit` 0 错误。临时脚本 qa_verify.mjs / qa_chain.mjs 已删。
- 提交：分支 `chore/admin-i18n`，commit `5433633`，含 4 交付文件 + 设计文档/ADR/spec/工单/agent 定义（novel-cms 基础）。未提交无关的 CLAUDE.md 改动与 .workbuddy/memory。
- 坑：`tsc --noEmit` 须用 `node node_modules/typescript/bin/tsc`（`.bin/tsc` 是 sh 包装，node 直跑报 SyntaxError）。
