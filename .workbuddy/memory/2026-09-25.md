# 2026-09-25

## 全仓功能面体检（10 类），输出修复规范（未实施）

- 分支 `feat/login-credential`，HEAD `66b420b`。规范落 `.scratch/microfeed-functional-audit/spec.md`，
  任务拆在 TaskList #6–#11（阶段 A/B/C 各一轮实施+复核）。**本轮零代码改动**，等用户"进入执行"。
- 方法：按 10 类切分（认证 / RBAC / 内容 / 编辑器 / 媒体站点文件 / 公开API / Webhooks / 主题 / CLI部署 / i18n前端规范），
  只读勘察后**逐条回读原文核实**再落方案。

### 核实结论（P0，均已回读原文确认）
1. **公开 API 三域零授权**：`src/server/api/api-permissions.ts` 的 `DOMAIN_RULES` 无 `pages`/`site-files`/`media_files`，
   `credential-bearer.ts:101` `permissionCode === null` 即放行。`/media_files/presigned_urls/` 确在 `OpenApiDocument.ts:733`。
2. **Webhook 六个读端点无守卫**：`ajax/webhooks/` 下 overview / endpoints/index / endpoints/[endpointId]/index /
   deliveries/index / deliveries/[deliveryId]/index / explorer/subjects 全裸 export GET，只有 `[endpointId]/secret.ts` 套了 `withWebhookGuard`。
3. **分章导入端到端不可用**：`ImportChaptersApp/index.tsx:96-104` 发 `content`（服务端 `FeedCrudManager.ts:74-80` 只认 `content_html`）→ 正文丢；
   未发 `date_published_ms` → `FeedDb.ts:623` `msToRFC3339(undefined)` → RangeError 每章 500；无 `bookId` → `book_id` 派生列 null。
4. **媒体存储型 XSS 面**：`src/server/media/media.ts:8-19` `objectHeaders` 无 nosniff，`media-upload/[...key].ts:71-73` 直接用上传方 Content-Type。
5. **重置密码不撤销登录凭证**：`password-setup.ts:347-375` 删了 oauth/session，无 `ext_login_credentials`（列 `user_id`，见迁移 0036）。
6. **用户名登录无限流**：`better-auth.ts:134-150` customRules 缺 `/sign-in/username`，落到默认 `max:100/60s`。

### 勘误（重要：勘察报告不可全信）
- ~~导入未生成 11 位 id~~ = **误报**。`FeedCrudManager.ts:173` `const itemId = item.id ? item.id : randomShortUUID();` 有兜底。
- 教训：子代理给的行号与结论必须回读原文复核，尤其"某字段未设置"类断言。

### 新增权限码的机械约束（写迁移时必守）
- `ext_permissions(id, code, name)`，`id` 由 code 派生 = `p_<':'→'_'>`；`ext_role_permissions.permission_id` 外键
  **仅 ON DELETE CASCADE、无 ON UPDATE CASCADE** → 改码必须「插新→拷授权→删旧」，不能直接 UPDATE（见 0057 注释）。
- 新增码须同步 `PERMISSION_CODES`(`Constants.ts`) + `rbac/seed.ts` 的 `RBAC_PERMISSIONS` + i18n 中文名，否则 `admin-endpoint-guards.test.ts` 红。
- **回归兜底**：新码须在同一迁移内授予既有相关角色（如授予所有持有 `content:site_file:manage` 的角色），否则既有凭证 403。

### 待用户决策三项（不得自行假定）
1. A3 导入是否加"书选择器"（默认最小修复，不归属书）
2. B2 是否新增 `content:audit:manage`（默认按动作分派，不新增码）
3. B15 后台是否发 `x-device-id`（默认本轮只加校验，不动）

## spec 第二版：并入「上游升级保障」维度（2026-09-25 上午，仍零代码改动）

- **文件归属三档**（用 `git log --diff-filter=A` 实测创建提交判定，合并前需 fetch 上游复核）：
  - A 本地特有（创建于 9 月本地提交）：`api-permissions.ts`/`credential-bearer.ts`/`login-throttle.ts`、`rbac/*`、`feed/ext*.ts`、`ImportChaptersApp/`、`Rbac.ts`、`admin-endpoint-guards.test.ts` → 随便改。
  - B 上游继承·本地深度分叉：`src/pages/api/auth/[...all].ts`、`src/middleware.ts`、`admin-routes.ts`、`admin/webhook-handlers.ts`、`ajax/webhooks/*.ts`、`OpenApiDocument.ts`、`api/access.ts` → 只追加调用/插行。
  - C 上游核心：`media.ts`/`uploads.ts`/`media-upload/[...key].ts`、`password-setup.ts`、`better-auth.ts`、`FeedDb.ts`/`FeedCrudManager.ts`、`http.ts`、`MediaFileUtils.ts` → 只插一行。
- **A6 改道**（关键）：用户名登录限流**不再改 `better-auth.ts`**（红线文件）。改在 `/api/auth` 唯一咽喉
  `src/pages/api/auth/[...all].ts` 的 `auth.handler(request)` 前**追加 1 行**调用本地新文件
  `src/server/auth/auth-endpoint-throttle.ts`（A 档）；限流存储用新迁移 `0059_ext_auth_throttle.sql`
  （`key/window_start_ms/count`）。5 次/60s，IP 缺失跳过 IP 维度（与 B13 同口径）。
- **迁移纪律**：只增不改；新迁移一律 `ext_` 前缀+语义名+幂等（`INSERT OR IGNORE`/`NOT EXISTS`/`CREATE TABLE IF NOT EXISTS`）；
  撞号按文件名去重（同号不同名不致命）。
- **合并后守护**：四门禁（AdminCredentials + worker auth/rbac/login-credential + username-login）+
  本 spec §10 各修复守护测试；红 = 语义静默回退，重新落地，不得反向改断言。
- spec 现含：§2.5 上游保障（三档表/五条硬规则/影响评估表/合并流程）、A6 改道方案、§4 迁移 0058/0059/0060、
  §7 架构补减叉层、§8-7 与 §10-77 合并演练收尾。实施清单编号重排为 1–77。

## 功能审计 阶段 A 复核 + 阶段 B 全量实施（B1–B27，2026-09-25 续）
- 阶段 A 复核通过：typecheck 修复 2 个 A4 自有错误（input.size 收窄、split[0] ?? ""）+ 补 2 个 webhook 守卫 WIP 漏导入；剩余唯一错误 = novel 测试（超范围，阻部署门禁）。
- 阶段 B 全部落地：B1 角色镜像、B2/B3/B4 守卫码对齐、B5 无需（A1 已消重）、B6 审计哨兵、B7 公开 API 全走 Accept-Language（新增 publicLocalizedTextError）、B8 content-read JSON 404、B9 OpenAPI 补 403/429（脚本插 21 操作+翻译表 7 条）、B10 i18n、B11 重试绑退避表+抖动、B12 迁移 0060 删 resolved_at 死列、B13 无 IP 跳过 IP 维度、B14 批内条件 INSERT（auditStatement 加 onlyIf 守卫）、B15 设备头校验、B16 删号清凭证、B17 新建即发布进审核（snapshot={}+强制 pin+空 snapshot 不回填 status）、B18 book_id 索引查询、B19 删分类清悬空、B20 删条目回收内嵌图片（itemMediaUrls）、B21 日志取真实 status、B22 OpenAPI 去实例域名（$MF_ORIGIN 占位）、B23 三处文案 i18n（PublicSearch strings 注入 + PublicSearchI18n.ts）、B24 maxLength 常量、B25 catch 分类 4xx/5xx、B26 五 GET 补守卫（feed/json 绑 chapter:read）、B27 迁移 0061 权限配置菜单+独立守卫页。
- 阶段 B 复核门禁：typecheck exit 0（仅 novel 测试 1 项超范围错误）、i18n:check 2017/2017、lint:openapi 通过。
- 偏差细节全部记录在 spec.md §12/§13。下一轮：阶段 C（C1'–C12'）→ 收尾（diff --check、提交信息交用户、部署+D1 复核、上游合并演练）。
- 教训：Edit 并发同文件会互相覆盖（老坑重现）；search/json.ts 曾写 `guard ?? fallback()` 漏 await Promise，两处当场发现改 async/await。

## 阶段 C 完成 + 部署（2026-09-25 终）
- C1'–C12' 全部落地（偏差见 spec.md §14）：C7' 对账测试抓到 3 张漏分类表（ext_api_access_log_v2/microfeed_installation→durable，ext_auth_throttle→ephemeral）已补 SNAPSHOT_TABLES；C12' 采用客户端标题键去重（/ajax/items?status=all 分页预取，未改 ajax/feed 契约）；C3' 新增 /rbac/audit 只读页+迁移 0062。
- novel 测试 ts(2345) 用 as number 修复（部署门禁解除）；typecheck 最终 0 errors、i18n 2026/2026、lint:openapi 过、diff --check 干净。
- 部署：deploy8.log，含迁移 0058-0062。教训：JS 正则没有 (?m) 内联标志（Python 习惯踩坑），用 /gmu；\"unknown!\" 断言得 {} 不匹配 number。
- 待办：部署完成后远程 D1 验证迁移落库 + commit 清单交用户（禁止自动提交）。
- deploy8 test:deploy 失败（api-item-service 6 例）→ 二分（stash 验证 HEAD 8/8 过）→ 三处修正：B17 整体回退（与已提交 API 契约冲突，转决策项：service.ts 不传 openReview 与全局开关的一致性）、A3 FeedCrudManager 回退（破坏"更新省略日期即保留"契约，import 客户端本就传日期）、C6' 改按协议拒绝（javascript:/data:，放行四种合法形态，形状白名单误杀裸遗留键 media/…）。修正后 worker 45/45、unit 11/11。
- deploy9 成功并验证；远程 D1 确认 0058-0062 落库、ext_menu 三行绑定码正确。
- 剩余待办：git 提交清单交用户手动执行（约 80 改动文件 + 13 新文件）；上游合并演练。

## 排查：/rbac 与 /rbac/permissions 两页"完全一样"（15:35）
- **确实一样**：两 `.astro` 仅差 3 处——页守卫码（role:manage vs permission:manage）、`activeNavItem`、
  i18n 标题键（"角色与权限" vs "权限配置"）；正文同为 `readRbacBoard` + `<RbacApp initialBoard={board} />`。
  `RbacApp` 不感知查看者权限，两页渲染同一个角色编辑器。
- **重复原因 = B27**（spec.md:363-364/604-606）：`ext_menu.permission_code` 一行只绑一个码、
  `menu.ts:77` 按单码过滤 → 只持 `system:permission:manage` 者无导航入口，而存授权端点正需该码。
  选了"新增菜单行+新页、一菜单一码"，未放宽校验。迁移 0061 已 deploy9 落库（生产已双页）。
- **三条真实缺陷**：① 复制了页面没复制能力——role:manage 单持者能进 `/rbac` 但存授权 403；
  permission:manage 单持者能存授权但角色 CRUD 403。② SSR 侧门：`/rbac/permissions` 只要
  permission:manage 却服务端返回全量 board，同一数据走 `GET /ajax/rbac` 需 role:manage。
  ③ `admin-page-guards.test.ts` 红（实跑确认）：测试只读 0041 菜单行，0061 新行不在 bound →
  "never guards a page with a code no menu binds" 失败于 `rbac/permissions/index.astro`。
- 附带：`ADMIN_URLS.ajaxRbacBoard` + `getAdminRbacBoard` 零调用（死代码）。

## 「/rbac 是管菜单显示」猜想 — 证伪 + 合码影响面（16:05）
- 证伪：`ext_menu` 在 src 下只有读（`menu.ts:36`、`rbac-handlers.ts:77`），零写入；菜单只能靠迁移改。
  `RbacApp` 用菜单只当权限树标签，无菜单编辑能力。两页都是同一个角色/授权编辑器，不是"菜单显示"vs"权限配置"。
- 远程 D1 实测：menu 4 行（rbac/rbac_permissions/users/rbac_audit）0061+0062 已落库；
  `ext_menu_permissions` 无 rbac_permissions 行 → **0063 未落库**（15:42 新建）；
  两个 system 码授予次数均为 **0**，仅 super_admin 持 `*`。合码的数据迁移风险 = 0。
- 全量单测：`qa_unit_full.log` 21 失败文件/137 失败用例，admin-page-guards 只是其中之一。

## 两轴代码评审（Standards/Spec）与后续修正
- 用 code-review 技能对 git diff HEAD 全量未提交改动做 Standards + Spec 双轴并行评审。**Spec 轴抓到 2 处我此前漏掉的真实回归**（都不在 test:deploy 门禁子集内，故部署与定向验证都没拦住）：
  1. B27/C3' 只写了 ext_menu 行，漏了 ext_menu_permissions 映射（0052 权限树数据源）→ admin-page-guards 测试红；修正：迁移 0063 补映射 + 该测试菜单来源改为读全部含 INSERT INTO ext_menu 的迁移（原只读 0041）。
  2. C7' 把 microfeed_installation 又加进 durable，但它本已属 targetSpecific → assertClassifiedTables 抛重复分类 → snapshot 18 例红；修正：从 durable 移除。
  3. A3 回退后其守护断言失效 → 改写为正确分层（导入载荷恒带日期 + 映射器对省略日期保持未设，守护"更新即保留"）。另修 extVolume 夹具缺 book_id 列。
- Standards 轴另修：OpenAPI /media_files/presigned_urls/ 补 403/413/415 + 翻译；migrations/README 如实界定"列级 DDL 不可重放"；ajax/feed.ts 权限码字面量改常量。
- 记为后续项：两个 throttle 模块同形可抽公共件；rbac/permissions 页与 rbac 页逐行重复；media-upload 语言解析可辩护未改。
- 教训（写进技能）：**部署门禁只跑 test:deploy 子集，不等于全量单测通过**——复核声明"通过"前必须跑与改动相关的单测套件（尤其 tests/unit 下不在门禁里的：admin-page-guards、server/extVolume、manage-cli/snapshot、import-chapters-payload）。
- 第二个红测试（实跑确认）：`tests/worker/admin-menu.test.ts:267` 菜单顺序期望数组未跟 0061/0062 更新
  （21 vs 23，多 rbac_permissions/rbac_audit）。worker 测试需 `--config vitest.worker.config.ts`。

## 评审待办落地（§16）+ 重复路由清理
- A6：抽 `src/server/auth/client-address.ts` 共享（cf-connecting-ip → x-forwarded-for 首跳 → null）；端点节流无地址时**跳过 IP 维度**（原全局单键，与 B13 一致）；两个窗口计数器刻意不合并并写明原因。
- 测试覆盖：api-permissions 改派生式（OpenAPI 的 (path,method) 探针，5→32 例）；admin-endpoint-guards 扫描拓宽到整个 [adminPath]（84 .ts），带两类豁免（会话自助 ajax/account、预认证 auth/login；纯 302 壳 channels|items/index），并新增**重导出目标解析**（ajax/rbac/* 的守卫在 rbac-handlers 内）。
- 去重：新增 `components/admin/rbac/RbacBoardPage.astro`，两 board 页各缩到 20 行（守卫留页内，供 admin-page-guards 解析）。
- **顺带修掉重复路由**：Ajax `books.ts` 与 `books/index.ts` 同占 `/[adminPath]/ajax/books`（同一提交引入，Astro 警告将来会变硬错误）→ 删 books.ts + 孤儿 listBookOptions；警告消失。
- 验证：typecheck 0 errors、36 套件 224 例全过、deploy12 上线并验证；线上复核 B22（openapi.json 无实例域名）与 B7（/search.json 按 Accept-Language 返回中/英）均通过。

## 安全提交与推送（fix/functional-audit）
- **`git-safe-commit` 技能不存在**（工作记忆有误，`~/.workbuddy/skills/` 无此目录）→ 按记忆中的规程手工执行：`git diff --check` 干净 + typecheck 0 errors + 相关套件 224 例通过 → 建分支 `fix/functional-audit`（不在 main 上提交）→ **每个 commit 后立即 `git rev-parse HEAD` 复核**。
- 6 个提交（中文 conventional）：958830b fix(api) 授权闭合/本地化/OpenAPI → 891287c feat(rbac) 守卫+菜单映射+审计页 → 5d4bfc1 fix(auth) 限流与凭证生命周期 → 0a9ae27 fix(webhooks,media,feed) 重试/死列/回收/索引 → 462e835 chore(audit) 阶段 C 清理+对账+重复路由 → d5f5d2d docs(audit) 规范与记忆。
- 已推送 `origin/fix/functional-audit`（本地=远端 d5f5d2d），`origin/main` 未触碰（b510bb2）；该分支相对 origin/main 领先 **129** 个提交（含此前未推送的 123 个本地提交）。
- PR 入口：https://github.com/andylxy/microfeed/pull/new/fix/functional-audit
- 方案复核（17:20）：分支已换 fix/functional-audit，新提交 891287c 等。过期项：admin-page-guards 已修绿（读全部迁移）、
  0063 已提交并落库（不能删文件，要写 0064）、删页反而会让 admin-page-guards ENOENT 报错。
  新增：共享组件 RbacBoardPage.astro 需改注释；admin-menu.test.ts 期望数组缺 rbac_audit，只删 rbac_permissions 仍红。
  未变：结论不变，远程两码授予仍为 0。

## 推送后回修（全量 worker 套件暴露 4 处红）→ deploy13 → 再次推送
- 教训落地：`test:deploy:worker` 只跑 7 个文件，**不含** admin-menu/rbac/login-credential/webhooks/auth。推送后跑全量 `vitest --config vitest.worker.config.ts tests/worker/`（25 文件）发现 4 红：
  1. **我的真回归**：B14 的批内条件 INSERT 漏 `AND revoked = 0` → 吊销凭证后无法再建（原 countLoginCredentialsForUser 明写只计未吊销行）。已修。
  2. admin-menu.test.ts 写死菜单顺序 → 补 RBAC_PERMISSIONS/USERS/RBAC_AUDIT。
  3. rbac.test.ts editor 授权 12→13（0058 的 media:file:manage，A1 预期）。
  4. login-credential.test.ts 旧用例断言「pages 无需码」= A1 要修的漏洞 → 改写为新契约（无授权 forbidden / super_admin allow）。
- 0063 的两行与 0052 已有映射重复，破坏「每码在权限树恰好一次」→ 写 **0064** 删重复行（0063 已落库不可改）；deploy13 落库并验证 D1 只剩 2 行。
- 全量 worker：**25 文件 293 例全过**；线上 `system:*` 五个码 granted_to 均为 0（仅 super_admin 可达），新增 /rbac/permissions 页同理——授不授权属运维决策。

## git 推送修复（GitHub 走本机出口代理）
- 回修两提交（4caef54 fix + dfb5bcd docs）+ 本记忆注记（295a781）已本地提交，但 `git push` 报 `CONNECT tunnel failed, response 502` / `SSL unexpected eof` 卡死。根因：本机出口依赖系统 IE/WinINET 代理 `127.0.0.1:10808`（浏览器走它，因此浏览器能访问 GitHub），Git for Windows 默认不读取该代理、改为直连；直连出口允许 fetch/pull、阻塞 push（receive-pack）。这属 Git 未配置代理，并非泛化网络故障。`ssh -T git@github.com` 与 `ssh -T -p 443 git@ssh.github.com` 均连通（仅缺密钥），证明 SSH 端口可达。
- 修复：`git config --global http.proxy http://127.0.0.1:10808`（与浏览器同一出口），之后 `git push origin fix/functional-audit` 正常完成，`244e741..295a781` 已上远端，3 提交全部到达。撤销：`git config --global --unset http.proxy`。
- 该代理由本机 VPN/代理客户端提供；客户端未启动时间接推送会失败（届时浏览器同样打不开 GitHub）。
- 执行：删 /rbac/permissions 页 + 合码（未提交）。新增迁移 0065（原 0064 与另一会话撞号，已让出）、
  ADR-0002；改 Constants/seed/handlers/StringUtils/i18n + 三个测试。门禁全绿（typecheck 0、i18n 2023/2023、
  unit 13/13、worker 82/82）。教训：ext_permissions 的 DELETE 要写 IN(…) 才被端点守卫测试解析；
  并发改 spec.md 会互相覆盖，故未改。
- deploy10 上线 0065 并验证：远程 D1 四个计数全 0（menu 行/权限码/映射）+ users sort 502 + d1_migrations 记账；
  dist 构建产物中 `rbac/permissions` 零命中。教训：未登录探测该路径返回 302→登录页，不能当路由存在的判据。
- 全量复核：worker 25/25 文件 292/292 全绿；unit 144/145 文件、1029 通过 / 3 失败，
  3 个失败全在 `tests/unit/cli/help.test.ts`（CLI 文案，与本任务无关）。零新增失败。
- **更正**：上一节"3 个 unit 失败"是假红——我用 `./node_modules/.bin/vitest` 绕过 yarn，导致
  `detectCliInvocation()` 判成全局安装，cli/help 断言 yarn 形态的 3 例失败。经 yarn 复跑：
  unit 145/145 文件 1032 通过 / 1 skipped / **0 失败**，worker 25/25 文件 292/292。整套全绿。
  铁律：本仓跑测试必须 `./node_modules/.bin/yarn vitest run`。
- 收尾：spec.md §4 台账补齐 0061–0065 五行（原只列到 0060），标注"0061→0065 净效果为零"。
  远程 D1 复核分组菜单确为 3 行：rbac(501)/users(502)/rbac_audit(504)。`git diff --check` 干净。
- 用户要求放开 AGENTS.md 的凭证条款。查清共三处（L36 提交 / L187 CLI / L271 部署），
  经用户选定「放开读取、禁外泄」：三类凭证可读，但不得打印进对话 / 写文件或日志 / 提交，
  且不得在对话中索取凭证。L36 加交叉引用，L271 保留索取禁令。核过
  `tests/unit/agent-guidance.test.ts`（读 AGENTS.md 的 6 技能名 + 禁 `Codex`）不受影响。
- 推送：远端 `fix/functional-audit` 已 = 本地 HEAD `295a781`，`Everything up-to-date`，
  **无内容可推**。真正未上去的是 20 项未提交改动。挂死真因 = Git Credential Manager 等 GUI，
  需 `GIT_TERMINAL_PROMPT=0 GCM_INTERACTIVE=never`。推送后 `.git/refs/remotes/origin/` 被沙箱搬走
  （`[gone]`），但分支引用与 fsck 均正常。
- 用户指令「先部署后，再安全提交git.推送」已完成：部署经 deploy10 确认已覆盖（无需重做）；
  两个提交 `fee3f15`(RBAC 收敛 16 文件) + `448df73`(AGENTS.md)，走 git-safe-commit，两次引用被吃两次恢复；
  推送 `295a781..448df73` 成功，ls-remote 复核远端 == 本地。排除 3 个记忆文件。
  推送真因：`credential.helper=helper-selector` 38s + GCM get 挂住，须 `-c credential.helper=` 清空后显式指向 GCM exe。
  教训：探测 GCM 凭据时我误把 `credential fill` 重定向进文件，明文 token 落盘，已截断清零并删除。
- 用户要求重新部署：`deploy54.log`，exit=0，`Checks and build passed [2m 16s]` + `Deployed and verified`。
  独立验证：线上 `/.well-known/microfeed.json` 与首页均 200；dist 中 `rbac/permissions` 零命中；
  远程 D1 五项全对（menu/perm/tree/grants 全 0，users_sort=502）。无迁移行（0065 已落库）。
- 内容读 API 回归测试：`python .microfeed/test_api.py` → **65 PASS / 0 FAIL**（exit=0），
  与 15:09 合并前基线逐行 diff 完全相同。静态核查确认零影响：码表在 `src/server/api/api-permissions.ts`
  （不在 rbac 目录），`content-read.ts` 与 rbac 零耦合，删除的两条路由都在 [adminPath] 下。
- /admin/rbac/ 权限树叶子改单列：`RbacApp/index.tsx:548` 由 `grid gap-2 sm:grid-cols-2`
  改为 `flex flex-col gap-2`（1 行）。用「复刻真实 DOM + 复用 dist 构建 CSS + CDP 量 boundingRect」
  实测：15 个页面 25 个叶子全部独占一行（books 由 [2,2] 变 [1,1,1,1] 等）。
  坑：探针选择器不能用正在被测的 class（0 匹配会假通过）；改完须强制重载页面。
  已固化为技能 `microfeed-admin-layout-verify`。
- 部署权限树单列改动：`deploy55.log`，exit=0，`Checks and build passed [2m 16s]` + `Deployed and verified`。
  独立验证：dist 中旧类名 0 命中、新类名落在 `dist/client/_astro/index.Di5liDT8.js`；线上 200 / `/admin/` 302。
