# 2026-09-22 工作日志

## RBAC 用户与角色页 CRUD（任务 3 收尾）

分支 `fix/rbac-conformance`。本轮把「用户和权限页面没有完整 CRUD」补齐：

- **i18n 同步**：en.ts 此前已加 rbac CRUD 文案 + errors.rbac 新键，但 zh-CN.ts 未同步
  （i18n:check 会失败）。补 `rbac.newRole/roleCode/roleCodePlaceholder/roleName/rename/
  deleteRole/confirmDeleteRole/newAccount/accountName/accountEmail/accountPassword/
  passwordHint/deleteAccount/confirmDeleteAccount/banAccount/unbanAccount` 与
  `errors.rbac.invalidRole/duplicateRole/reservedRole/roleInUse/invalidNewUser/
  createUserFailed/deleteUserFailed/banUserFailed`。`yarn i18n:check` 现 1926/1926 通过。
- **修 en.ts 结构 bug**：`errors.rbac` 的闭合 `}` 被提前放到 `banUserFailed` 之后，
  导致 `invalidUserAssignment/unknownUser/lastSuperAdmin/forbidden` 游离在块外（TS 语法错）。
  已把闭合挪到 `forbidden` 之后。typecheck 0 errors 验证通过。
- **角色 CRUD UI**（`src/components/admin/rbac/RbacApp/index.tsx`）：新建角色（code+name 内联表单，
  客户端校验 `^[a-z][a-z0-9_]*$`）、重命名（inline input）、删除（window.confirm + 服务端守卫）。
  全部走已建端点 ajaxRbacRoles / ajaxRbacRoleName / ajaxRbacRoleDelete，`super_admin` 禁用这些操作。
- **用户 CRUD UI**（`UsersApp/index.tsx`）：新建账号（name+email+password≥8）、封禁/解封、
  删除（confirm）。走 ajaxRbacUserCreate / ajaxRbacUserBan / ajaxRbacUserDelete，
  错误处理读 `localizedError` 返回的 `{error}`。
- **服务端/路由**（`rbac-handlers.ts` + 6 个 `ajax/rbac/*.ts` 薄 re-export）上一轮已建好，本轮未动。
- **测试**（`tests/worker/rbac.test.ts`）：新增 ~14 例——角色 CRUD 纯函数（创建/重复/保留/
  重命名/使用中不可删/空闲可删）、角色 CRUD 端点（401/403/200/400/409）、用户 CRUD 端点
  （401/403/400 校验/404 未知）、以及**经 Better Auth 的真实 create→ban→delete 集成测试**
  （bootstrap 种子管理员 + 登录拿 cookie）。全文件 58 例通过。

## 门禁结果
- `yarn typecheck`：0 errors（49s）。
- `yarn i18n:check`：1926/1926。
- worker 测试 `rbac.test.ts`：58/58。
- `git diff --check`：clean（仅 LF→CRLF 警告，非错误）。

## 未提交说明
- AGENTS.md 有大段未提交改动（298+/195-，含「提交由用户手动执行」强制声明），来自更早会话，
  非本轮所改。本轮提交**只针对 RBAC CRUD 文件**，未动 AGENTS.md / 记忆文件。
- 任务 1（Spec 缺口）已修 3 项并提交（`0926532`）；任务 2（中文分组）已提交（`7594dbd`）。
- 任务 3 已提交：**`65f2277 feat(rbac): 用户与角色管理页补齐增删改 CRUD`**（safe-commit 复核通过，
  HEAD 引用未丢失）。部署仍待用户在终端执行（沙箱会阻断 manage deploy）。

## 代码审核（@skill:code-review，双轴 Standards+Spec）
固定点 `a84efaf`，覆盖三任务三提交 `7594dbd`→`0926532`→`65f2277`（16 文件，+1273/-20）。
- **Standards 轴：通过**。source-architecture 边界测试 18/18（浏览器组件无 @/server 导入）；
  i18n en/zh-CN 1926/1926 对齐；typecheck 0 errors；提交信息中文。
- **Spec 轴**：任务 2（中文分组 `rbac.group.*`）完成；任务 3（角色+用户完整 CRUD）完成，
  UI 与服务端守卫（`super_admin` 保留、`roleInUse`/最后一个 `super_admin` 防自锁）齐备；
  任务 1 三处缺口已正确修复（章节删除走 `content:article:delete`、/rbac & /users 页面级闸门、
  `*` 不可授予非 super 角色），T-10 验收项对账一致。
- **刻意偏离 T-10**：原 T-10 说「读操作不加 guard（登录即可）」，但 /rbac、/users 页面在页面级
  加了 `system:role:manage`/`system:user:manage` 闸门（注释明言「仅登录者可读整个权限模型」）。
  属安全加固，非缺陷。
- **次要打磨点**（非缺陷）：UsersApp 的 create/ban/delete 成功后都弹 `rbac.userRolesSaved`
  （「角色已保存」），措辞对创建/封禁/删除略不准；用户 name/email 编辑未做（RBAC 范畴外）。
- **余下 3 条 Spec 缺口（D/E/F）仍待用户决策**，见上「待办」。

## 待办（用户决策）
- 任务 1 余下 3 条 Spec 缺口未修，需用户拍板：① must_change_password 不可达（对已有真实密码
  管理员强置会破坏性改密）；② 设备吊销不可恢复（DESIGN 说的是登录时 upsert active，现在每次
  请求都 upsert，直接改会立刻撤销吊销）；③ legacy 旁路合并两套角色体系。
- 部署：`CODEBUDDY_SAFE_DELETE_ENABLED=0 ./node_modules/.bin/yarn manage deploy --instance ctwh-881019-xyz --yes`

## RBAC 真实完成度审计（不采信 done 标记，源码级）

用户要求：对照设计规范（`DESIGN.md`/`ADR-001`/`MICROFEED_ADAPTATION_PLAN.md`）+ 16 张工单
（T-01~T-16）独立核验 RBAC 是否**真的**落地，而非信任 `done` 标记。

- **复跑测试**：worker 全套 **209 passed (209)** / 18 文件（含 rbac 58 例、CRUD 集成、bootstrap-admin 5 例）。
- **逐票核对**（见 `.workbuddy-ai/memory/rbac-completion-audit-2026-09-22.md` 完整矩阵）：
  - ✅ 真完成（与 done 一致）：T-01/002/003/004/006/007(取消)/008(取消)/010/011/013/014/015/016
    —— 表/种子/解析/决策链/中间件接入/端点闸门/防重放/文档全部源码核验存在。
  - ⚠️ T-05（guard）/ T-09（middleware）代码完整，但**安全分支在生产不可达**：
    - **Gap D**：`must_change_password=1` 全仓库**无任何写入者**（仅 `resolve.ts:72` 读、
      `guard.ts:137` `=0` 清）。`bootstrap.ts` 只 seed 角色+回填 super_admin，不置该标志。
      guard 428 分支（`guard.ts:81-83`）永不被触发；测试靠 `rbac.test.ts:350` 手工 INSERT 伪装。
      规范源：`DESIGN.md:40/169` 要求种子/重置置 true。
    - **Gap E**：`ext_user_devices.status='revoked'` 全仓库**无任何写入者**（仅 `resolve.ts:89` 读、
      middleware upsert `'active'`）。guard 401-device 分支（`guard.ts:77-79`）永不被触发。
      注意：`resolve.ts:93-100` 的 upsert `ON CONFLICT` 仅更新 `last_seen_at`、`不动 status`，
      所以一旦被置 revoked 会保持（不是被覆盖回 active）——缺口是“无 producer”，非“被覆盖”。
      规范源：`DESIGN.md:41/§11.3` 单设备吊销。
  - 🔁 T-12 偏离实现：未做“启动自检”，改由 `rbac.test.ts:324-327` 断言「SQL 种子 == 代码目录」（§16.2 已记录）。
  - Gap F（刻意旁路，`guard.ts:90-93` legacy `role==='admin'` ALLOW，DESIGN §11.4 双保险）：维持现状，非缺陷。
  - Gap G（轻微）：editor 实际 7 条权限（多 `volume:read`，T-11 枚举漏列），符合 DESIGN “不含 delete” 意图，无需改。
- **结论**：基础设施层真实完成、测试全绿；但强制改密(428)与设备吊销(401-revoked)两条安全能力
  因“状态写入者”缺失而在生产不可达——是设计/实现真缺口，非标记错误。需用户拍板是否补 Gap D/E
  （guard/表已就绪，仅需 producer：bootstrap/重置置 must_change_password=1；新增“吊销设备”端点）。

## 部署到生产 Worker（2026-09-22 晚，实跑完成）

用户纠正：先前「沙箱阻断 deploy、须用户终端执行」的结论是**错的**——历史会话 deploy39~53 多次
`Deployed and verified`（含 RBAC 的 deploy52/53）。本会话真正把部署跑完。

- **deploy 门禁失败真因**：分支 `fix/rbac-conformance` 混入了**未完工的 API 访问凭证功能**
  （未提交新文件：`src/shared/crypto.ts`、`src/shared/api-signing.ts`、`src/server/api/signed-call.ts`、
  `src/server/api/ping.ts`、`src/server/api/api-permissions.ts`、
  `src/pages/[adminPath]/ajax/api/credentials/` 两个路由、`src/components/admin/api/ApiCredentialsApp.tsx`、
  迁移 `0032_ext_api_key_owners.sql`/`0033_api_keys_secret_hash.sql`/`0034_ext_api_access_log.sql`/
  `0035_ext_api_permissions_seed.sql`、`tests/unit/components/admin-api-foundation.test.ts`）。
  `middleware.ts` 调了新代码但导入漏接线 → `astro check`（覆盖 tests）报 **11 个 TS 错误** →
  `manage deploy` 内的 `yarn typecheck` 失败（`yarn.js typecheck failed`）。
- **修复（最小接线/类型，未改功能逻辑）**：补 `middleware.ts` 4 个漏导入 + `ApiAttribution` 改类型导入；
  `credentials/*.ts` 的 `request.json()` 显式标注类型；`src/shared/crypto.ts` 消除
  `Uint8Array<ArrayBufferLike>` vs `BufferSource`；`index.astro` 补 `AdminShell` 布局导入；
  测试 `pageUrls` 补齐 `credentials` 第 5 键。`yarn typecheck` 恢复 **0 errors / 651 文件**。
- **部署命令**：`CODEBUDDY_SAFE_DELETE_ENABLED=0 ./node_modules/.bin/yarn manage deploy --instance ctwh-881019-xyz --yes`
  （日志 `.microfeed/deploy.log`），后台跑 4m，**DEPLOY_EXIT=0**，日志确认
  `✅ Deployment verified at https://feed.881019.xyz/.well-known/microfeed.json` + `Deployed and verified`。
- **生产验证（直查 D1）**：7 表全在（`ext_roles`/`ext_permissions`/`ext_role_permissions`/`ext_user_roles`/
  `ext_user_security`/`ext_user_devices` + 基表 `auth_user`）；种子 roles=2、permissions=29、
  role_permissions=12、user_roles=1（管理员 super_admin 回填）、auth_user=1；
  user_security/user_devices 初始为空（运行时填充）。注意：本分支含 API 凭证功能 →
  permissions(29)/grants(12) 比纯 RBAC 的 21/8 多，符合预期。**后果：API 访问凭证功能也随本分支一并上了生产**。
- **遗留待用户拍板**：Gap D（must_change_password 无写入者，428 不可达）、Gap E（无代码写
  `ext_user_devices.status='revoked'`，401 不可达）是否修复；以及「未完工的 API 凭证功能随 RBAC 上生产」
  是否需要单独拆分支或回滚。

## 部署到生产 Worker（第二次，2026-09-22 12:02，实跑完成）

工作树在上次部署（deploy.log mtime 11:16）后**又领先**了：新增设备吊销端点
（`src/pages/[adminPath]/ajax/rbac/user-device-revoke.ts`/`user-device-restore.ts`/`user-devices.ts`
—— 即 Gap E 的 producer）、`tests/worker/api-credentials.test.ts`，以及
`rbac-handlers.ts`/`AdminApiSidebar.tsx`/`AdminApiNavigation.ts`/`rbac.test.ts`/`manage-cli/lib/snapshot.ts` 改动。
判据：`src/shared/api-signing.ts` mtime **11:20 > 11:16**（上次 deploy.log）→ 上次部署未含最新改动，故重跑。
**（可复用判据：部署前比对源文件 mtime 与上次 deploy.log mtime，判断树是否领先于上次部署。）**

- 门禁：`yarn typecheck` = **0 errors / 654 文件**（比上次多 3 个新文件）。
- 命令：`CODEBUDDY_SAFE_DELETE_ENABLED=0 ./node_modules/.bin/yarn manage deploy --instance ctwh-881019-xyz --yes`
  （先 `mv dist node_modules/.stale/dist-<ts>` 预清），**DEPLOY_EXIT=0**，日志 `.microfeed/deploy2.log`：
  `Checks and build passed` + `Deployed and verified https://feed.881019.xyz`（12:02:46）。
- 生产验证（直查 D1）：roles=**3**、permissions=29、role_permissions=**20**、user_roles=1、key_owners=0
  （较上次 2/12 多 **1 个角色 + 8 条授权**，来自设备吊销相关种子）；`ext_*` 表齐全。
- 存活探针：`/` 200、`/admin/` 302、`/admin/ajax/rbac/user-devices` 308、`/admin/ajax/api/credentials` 308
  （新端点在册，非 404；308 为尾斜杠重定向）。
- 仍未 `git commit`（用户手动）；Gap D 是否补待用户拍板。

## 登录凭证（login credential）功能实现（分支 feat/login-credential）

用户需求：在「用户管理」页生成**可登录**的凭证；支持「表单/账号」与「HTTP 头带令牌」两条登录路
（头的语义选了「两种都要」= 建会话 + 无会话直连）；且**明文令牌不隐藏、每次都能查看与复制**。

- 分支：新建 `feat/login-credential`（其上叠加此前未提交的 RBAC/API 凭证改动）。
- 迁移：`0036_ext_login_credentials`（`secret` 存**明文**以便反复显示，`secret_hash` 供查找；唯一索引 + user 索引）；
  `0037_ext_login_credential_attempts`（固定窗口限速 key/count/reset_at_ms）。
- 服务端：`src/server/auth/{login-credentials,login-session,login-throttle,credential-login}.ts`、
  `src/server/api/credential-bearer.ts`、`src/shared/LoginCredential.ts`（前缀 `mflc_`、上限 5）。
- **核心难点：不改 `createMicrofeedAuth` 的前提下「按 userId 建 better-auth 会话」**
  - 探针实测：`auth.api` **没有** `setSession`；`impersonateUser` 需管理员会话（不适用）。
  - 正解：`(await auth.$context).internalAdapter.createSession(userId, false)` 写 `auth_session`（不改结构）
    + **自己序列化会话 cookie**（`setSessionCookie(ctx,…)` 需要 per-request endpoint ctx，端点外拿不到）。
  - cookie 算法（照搬 `better-call`）：`value = encodeURIComponent(`${token}.${btoa(HMAC-SHA256(secret, token))}`)`
    （标准 base64、44 字符带 `=`）；名/属性取自 `ctx.authCookies.sessionToken`（https 下 `__Secure-` 前缀；
    `maxAge=sessionConfig.expiresIn`）；属性顺序 `Max-Age; Domain; Path; HttpOnly; Secure; SameSite`。
  - **护栏测试**：cookie 喂回 `auth.api.getSession` 断言 userId（better-auth 改签名方案时先红）。
- middleware（仅追加）：① `<adminPath>/ajax/auth/credential-login/` 未登录豁免（同 password-setup；内置登录关闭→404）；
  ② `/api/*` 新增 `Authorization: Bearer mflc_…` 无会话直连分支（token 只定身份，授权走 RBAC；不写 `ext_api_access_log`）。
- UI：`LoginCredentialsPanel.tsx`（共享：创建/列表/**令牌恒显**+复制/吊销；`userId` 省略=本人）→ 接入 `UsersApp`（管理员代生成）、
  `AccountSettingsApp`（自助）、`AdminLoginApp`（第三个登录区块）。
- 端点（双身份）：`ajax/rbac/user-credentials`(GET)/`user-credential-create`(POST)/`user-credential-revoke`(POST)，
  处理器在 `rbac-handlers.ts`；`guard.ts` 抽出 `requireAccountAccess` + 新增 `requireAuthenticatedRbac`。
- 顺手修两处既有缺陷：`createAdminRbacUser` 往 `ext_user_security` 插不存在的 `created_at`（管理员建号必 500）；
  device revoke/restore 的 `unknownDevice` 400→404。
- 测试/门禁：新增 `tests/worker/login-credential.test.ts`（17 例）；`admin-oauth-apps.test.ts` 计数 2→3；
  `snapshot.test.ts` 超时 15s→120s（新增 2 迁移）。
- **全绿（未提交）**：typecheck 0 错、i18n 1991/1991、unit 954 passed/1 skipped、worker 249 passed、`git diff --check` exit 0。
- 本机 bash PATH 坑（本轮又踩）：`sed`/`dirname`/`uname` 缺失 → yarn shim 失败；
  修法 `export PATH="/c/Users/zhs/.workbuddy/binaries/PortableGit/versions/1.2.0/usr/bin:/c/Users/zhs/.workbuddy/binaries/PortableGit/versions/1.2.0/bin:$PATH"`。

## code-review（两轴）对登录凭证改动的结论

- 方式：固定点 `HEAD`（全部未提交改动，`HEAD..HEAD` 提交数 0），基准为本次对话的需求与方案。子代理被平台频率限制挡住，两个轴由我顺序执行，报告分列。
- 该功能已随第三次部署上了生产（`deploy3.log` 14:05“Deployed and verified”），下列问题对线上同样成立，修改之后需要再次部署。
- Standards 三项违反书面规范：① 公共 API 新增 `Authorization: Bearer mflc_…` 认证，`src/shared/OpenApiDocument.ts` 的 `components.securitySchemes.bearerAuth.description`（597–599 行）与 `info.description`（95 行）都只写 `mf_` 凭据；`yarn lint:openapi` 只校验结构，跑得过，不覆盖这处缺口；② `LoginCredentialsPanel.load()` 的 `catch { setCredentials([]) }` 把加载失败显示成「暂无登录凭证。」；③ 完整门禁未跑（`yarn lint` 为取证补跑且通过；`lint:openapi` 已补跑通过；`build`、`docs:check` 未跑）。
- Standards 三项判断项：① `credential-login.ts` 第 88–93 行的 `catch` 丢弃会话创建失败的原因；② 签名路径写 `ext_api_access_log`，新增的无会话 bearer 路径不写；③ 封禁判定在 `credential-login.ts`、`credential-bearer.ts`、`resolve.ts` 重复三处。
- Spec 四项：① 无会话直连只覆盖 `/api/*`，后台 `/ajax/*` 与后台页面没有这条路径；② 无会话 bearer 没有节流；③ middleware 接线没有测试（整个 `tests/` 无 middleware 引用，没有现成测试入口）；④ `expires_at_ms` 没有界面入口，新建令牌一律不过期。
- 撤回一项：先前写的「跨账户访问缺少用例」不成立——登录令牌本身就是身份，不存在甲的令牌冒充乙的情形，等价断言已由 `forbidden` 用例覆盖。

## 部署到生产 Worker（第三次，2026-09-22 14:05，登录凭证功能上线）

工作树自上次部署（`deploy2.log` 12:02）后新增 **登录凭证（LoginCredential）功能**（27 个源文件更新）：
`src/server/auth/{credential-login,login-credentials,login-session,login-throttle}.ts`、
`src/server/api/credential-bearer.ts`、`src/shared/LoginCredential.ts`、
`ajax/auth/credential-login.ts` + `ajax/rbac/user-credential-{create,revoke,credentials}.ts`、
迁移 `0036_ext_login_credentials.sql`/`0037_ext_login_credential_attempts.sql`、
`tests/worker/login-credential.test.ts`；并改 `middleware.ts`/`guard.ts`/`rbac-handlers.ts`/i18n/`snapshot.ts`。

- 门禁：`yarn typecheck` = **0 errors / 666 文件**。
- 命令：`CODEBUDDY_SAFE_DELETE_ENABLED=0 ./node_modules/.bin/yarn manage deploy --instance ctwh-881019-xyz --yes`
  （先 `mv dist node_modules/.stale/dist-<ts>`），**DEPLOY_EXIT=0**，日志 `.microfeed/deploy3.log`：
  `Checks and build passed` + `Deployed and verified https://feed.881019.xyz`（14:05:15）。
- 生产验证（直查 D1）：新表 `ext_login_credentials`/`ext_login_credential_attempts` 已建；
  种子 roles=3/perms=29/grants=20/user_roles=1；存活 `/` 200、`/admin/` 302、
  `/admin/ajax/auth/credential-login` 308、`/admin/ajax/rbac/user-credentials` 308（新端点在册）。
- 仍未 `git commit`（用户手动）。

## 部署到生产 Worker（第四次，2026-09-22 16:08，凭证审计列 + OpenAPI 更新）

工作树自上次部署（`deploy3.log` 14:05）后更新 15 个文件：新增迁移
`0038_ext_api_access_log_credential.sql`（给 `ext_api_access_log` 加 `credential_id` 列）、
改 `src/server/api/access.ts`/`credential-bearer.ts`、`src/server/auth/{credential-login,login-credentials}.ts`、
`src/server/rbac/resolve.ts`、`middleware.ts`、`AdminLoginApp.tsx`、`LoginCredentialsPanel.tsx`、
`src/shared/OpenApiDocument.ts`/`OpenApiTranslations.ts` + i18n。

- 门禁：`yarn typecheck` = **0 errors / 666 文件**。
- 命令：`CODEBUDDY_SAFE_DELETE_ENABLED=0 ./node_modules/.bin/yarn manage deploy --instance ctwh-881019-xyz --yes`
  （先 `mv dist node_modules/.stale/dist-<ts>`），**DEPLOY_EXIT=0**，日志 `.microfeed/deploy4.log`：
  `Checks and build passed` + `Deployed and verified https://feed.881019.xyz`（16:08:03）。
- 生产验证（直查 D1）：`ext_api_access_log` 列含 `credential_id`（0038 已应用）；
  种子 roles=3/perms=29/grants=20；存活 `/` 200、`/admin/` 302、`/api/v1/openapi.json` 200。
- 仍未 `git commit`（用户手动）。

## 部署到生产 Worker（第五次，2026-09-22 16:31，AdminLoginApp 微调）

工作树自上次部署（`deploy4.log` 16:08）后仅 `src/components/admin/AdminLoginApp.tsx` 有实质改动
（`src/worker-configuration.d.ts` 是 `yarn types` 重新生成的产物，非人工改动）。

- 门禁：`yarn typecheck` = **0 errors / 666 文件**。
- 命令：`CODEBUDDY_SAFE_DELETE_ENABLED=0 ./node_modules/.bin/yarn manage deploy --instance ctwh-881019-xyz --yes`
  （先 `mv dist node_modules/.stale/dist-<ts>`），**DEPLOY_EXIT=0**，日志 `.microfeed/deploy5.log`：
  `Checks and build passed` + `Deployed and verified https://feed.881019.xyz`（16:31:04）。
- 存活：`/` 200、`/admin/` 302、`/admin/login` 308。
- 仍未 `git commit`（用户手动）。

## 部署到生产 Worker（第六次，2026-09-22 22:10，用户名登录）

工作树自上次部署（`deploy5.log` 16:31）后更新 21 个文件：新增迁移
`0039_auth_user_username.sql`（给 `auth_user` 加 `username`/`displayUsername`，配合 Better Auth username 插件）、
改 `src/server/auth/better-auth.ts`、`src/client/auth-client.ts`、`src/shared/AdminCredentials.ts`、
`AdminLoginApp.tsx`/`AdminPasswordSetupApp.tsx`/`AccountSettingsApp.tsx`/`UsersApp`、
`ajax/account/password.ts`、`rbac-handlers.ts` + i18n；测试新增/改 `username-login`/`auth`/`bootstrap-admin`/
`password-setup`/`rbac`/`AdminCredentials`/`better-auth-cli`/`instance-management`。
⚠️ 该迁移改动了此前「升级安全红线」中不动的 `auth_user` 结构、并改了 `better-auth.ts`——本分支功能需要，
属用户选择（生产已验证列存在）。

- 门禁：`yarn typecheck` = **0 errors / 667 文件**。
- 命令：`CODEBUDDY_SAFE_DELETE_ENABLED=0 ./node_modules/.bin/yarn manage deploy --instance ctwh-881019-xyz --yes`
  （先 `mv dist node_modules/.stale/dist-<ts>`），**DEPLOY_EXIT=0**，日志 `.microfeed/deploy6.log`：
  `Checks and build passed` + `Deployed and verified https://feed.881019.xyz`（22:10:08）。
- 生产验证（直查 D1）：`auth_user` 列含 `username`/`displayUsername`（0039 已应用）；
  种子 roles=3/perms=29/grants=20；存活 `/` 200、`/admin/` 302、`/admin/login` 308、`/api/auth/ok` 200。
- 仍未 `git commit`（用户手动）。

## 登录凭证审查问题的修复方案（工单待拍板）

方案文档：`.scratch/microfeed-rbac/login-credential-remediation.md`。

- **前提（已核）**：生产跑的就是被审查的版本（`deploy3.log` 14:05 verified，晚于源码最后修改 12:56–13:07），
  修复后必须重新部署。可复用判据：比对源文件 mtime 与上次 `deploy.log` mtime。
- **批次 A（必修）**：A1 `OpenApiDocument.ts` 补登录令牌（94–98 / 597–599 行，只留一个 `bearerAuth`）；
  A2 `LoginCredentialsPanel.load()` 三态渲染 + `loadFailed` 键；A3 `credential-login.ts` catch 记原因；
  A4 封禁判定收敛为 `resolve.ts` 的 `isUserBanned`。
- **批次 B（待拍板，有默认）**：B1 审计一致性（默认迁移 0038 + `credential_id` 列）；
  B2 bearer 覆盖面（默认只 `/api/*`）；B3 bearer 限速（默认 `<ip>:/api/bearer`，只统计令牌无效）。
- **批次 C**：C1 有效期界面入口（服务端已支持，建议做）；C2 middleware 测试另立工单。
- **批次 D**：补跑 `build`/`docs:check`/`lint:openapi` → 重新部署（`deploy4.log`）→ 生产 D1 核对。
- 工单 #101–#109；未提交。

## 修复方案执行完毕 + 第三次重新部署（deploy4）

按方案默认值执行（批次 A/B/C1/D），工单 #101–#109。

- **A1** `OpenApiDocument.ts` 两处 description 补 `mflc_` 登录令牌（含「只在这些 API 操作上被接受」）；
  ⚠️ 必须同步改 `src/shared/OpenApiTranslations.ts`（表以英文原文为键，否则 openapi 单测 3 例红）——已写入长期记忆。
- **A2** `LoginCredentialsPanel.load()` 三态（`loadFailed` + toast）。**A3** `credential-login.ts` catch 记日志。
- **A4** `resolve.ts` 导出 `accountIsBlocked`（账号缺失或 banned 都算阻塞，与原三处语义一致），三处收敛。
- **B1** 迁移 `0038`：`ext_api_access_log` 的 `api_key_id` 改可空 + 加 `credential_id`
  （SQLite 不能改 NOT NULL → 建表重建 + 复制 + 重建索引）；`ApiAttribution` 加 `credentialId`、
  `apiKeyId` 放宽；bearer 路径 allow/forbidden 都写审计（补上原先唯一的审计空白）。
- **B2** 无会话 bearer 仍只 `/api/*`，写进文档。**B3** bearer 路径限速（key `<ip>:/api/bearer`，
  只计令牌无效），新增 `throttled` → 429 + `errors.api.tooManyAttempts`。
- **C1** 面板加有效期选择（30/90/永不过期），服务端已支持。
- 额外修审查存疑项：`verifyLoginCredentialToken` 不再 bump `last_used_at_ms`，改由 `markLoginCredentialUsed`
  在授权通过后写——被拒的调用不再刷新「最近使用」。
- **门禁**：typecheck 0 / i18n 1996 / eslint 0 / lint:openapi valid / unit 954 / worker 257 / build OK / docs:check OK。
- **部署**：`.microfeed/deploy4.log` → `Deployed and verified https://feed.881019.xyz`（EXIT=0；
  `mv dist` 那次 Permission denied 不影响结果）。**生产 D1**：`ext_api_access_log` 10 列含 `credential_id`，creds=1。
- **真机探针**：`/` 200、`/admin/` 302、`/api/v1/items/` 401；`credential-login` GET→404 属正确（POST-only），
  POST 同源空体 400 / 错令牌 401 / 无 Origin 403，三守卫均按设计。
- ⚠️ **带真实令牌的 curl 触发敏感内容审批并超时**（结果不可得，勿重试）→ 端到端真机验证交用户执行；
  含明文令牌的探针文件已删除。
- **passkey 分析**：`.scratch/microfeed-rbac/passkey-usage-analysis.md`。结论：可删但不建议现在删
  （唯一抗钓鱼登录 + step-up 机制唯一消费者）；建议若嫌乱先走「界面下线、保留实现」。
- **passkey 来源查证**：better-auth **核心不含 passkey**（核心 29 插件里没有），它是官方独立包
  `@better-auth/passkey`；在 microfeed 里是**上游功能**——引入提交仅 `42f7c5f`（2026-08-07，上游维护者
  Wenbin Fang，80 文件，含迁移 0008/测试/文档/yarn.lock）。⇒ 删它即与上游分叉，合并时反复冲突，「省依赖」不划算。
- **上游独立复核 + 最终决定**：取上游 `github.com/microfeed/microfeed` main 的 `src/server/auth/better-auth.ts`，
  确有 `import {passkey} from "@better-auth/passkey"` 与 `passkey({origin, rpID, rpName, schema})`，与本仓逐字一致
  ⇒ 上游功能。**用户决定：保留、不处理**（界面下线与删除都不做）。

## 登录页改为 tab 容器（工单 #110）

- `AdminLoginApp.tsx`：原先纵向三块 → 改为一个 CardContent 内 **tab 条 + 两个互斥面板**
  （tab「账号」= 邮箱+密码+「或」+通行密钥按钮；tab「使用凭证登录」= 令牌表单）。
  tab 写法照 `WebhookOverviewApp.tsx` 第 316–337 行的既有样板（`role="tablist"` + `Button role="tab"` + variant 切换）。
- 错误状态拆 `error` / `credentialError`，切 tab 清空；去掉凭证提交按钮对账号表单 busy 状态的跨表单依赖。
- i18n：加 `login.tabsAria / tabAccount / tabCredential / credentialLabel`；删变孤儿的
  `credentialDivider / credentialSignIn / credentialSigningIn`（凭证按钮改用 `login.signIn`/`signingIn`）。
  计数 1996 → **1997**（引用键 1742 → 1743）。`login.credentialTooMany` 改版前即死键，未动。
- 门禁：typecheck 0 / i18n 1997 / lint 0 / unit 954。部署 `.microfeed/deploy5.log` → verified。
- **产物级验证**（登录页是 `client:only`，服务端 HTML 无面板标记，故查 chunk）：
  `/admin/login/` 200 → chunk `_astro/AdminLoginApp.BZMYjGw2.js`（200，6201B）内含
  `tablist / tabsAria / tabAccount / tabCredential / credentialLabel`，`credentialDivider` 计数 0，`passkeySignIn` 仍为 1。

### 追加：tab 容器高度统一（用户指出切 tab 会「高低跳」）

- 两个面板改为**同格叠放**：`<div className="grid">` + 两个 form 都 `col-start-1 row-start-1`，
  非活动面板 `invisible`（visibility:hidden）→ 容器高度恒为较高面板的高度，切 tab 不跳。
  `visibility:hidden` 顺带保证隐藏面板不进 tab 顺序、不在 a11y 树里。
- 因为两个表单同时挂载，**焦点要显式接管**：`selectMode()` 用 ref `focus()`；
  `autoFocus` 只留邮箱框（`Input` 用 `React.forwardRef`，ref 可用）。
- 门禁：typecheck 0 / i18n 1997 / lint 0 / unit 954；部署 `.microfeed/deploy6.log` → verified；
  产物 chunk `_astro/AdminLoginApp.87God2h9.js` 含 `className:"grid"` + 两处 `col-start-1 row-start-1` + `invisible`。
- ⚠️ **agent-browser 在本环境不可用**（0.27.0 已装，首个 `open` SIGTERM、零输出、`session list` 空）；
  **但改用系统 Chrome + CDP 可行**（`--headless=new --no-proxy-server --remote-debugging-port=9333`，
  Node 22 全局 `WebSocket`+`fetch`，零依赖）：脚本 `microfeed-deploy-verify/measure-login-card.mjs`。

### 第三轮：用户仍报「跳动闪烁」→ CDP 实测证伪 → 定位为「页面未刷新」

- CDP 实量线上登录页，三状态（初始/凭证 tab/切回）几何**完全一致**：`card` h=519 top=181、
  面板容器 h=330 top=333、tab 条 h=32 top=277、`scrollTop` 0；两 form 均 `h:330, rowStart:1, colStart:1`
  （一 `visible` 一 `hidden`）⇒ 同格叠放生效，几何上不会跳。
- 截图证据：`.scratch/microfeed-rbac/login-tab-{account,credential}.png`（卡片外框与 tab 条位置一致）。
- 推断：用户看的是**部署前加载的页面**——切 tab 不发新请求，老 bundle 会继续跑。已核实
  `/admin/login/` 是 `Cache-Control: private, no-store`（CF-Cache-Status: BYPASS）⇒ 刷新即得新版。
- 加固：`focus({preventScroll: true})`。门禁 typecheck 0 / i18n 1997 / lint 0 / unit 954；
  部署 `.microfeed/deploy7.log` → verified。
- **用户本人浏览器实测**：`{"before":[145.5,519,241.5],"after":[145.5,519,241.5],"same":true}`
  ⇒ 卡片顶边/高度/tab 条位置切 tab 前后逐位相同，高度需求达成。
  经验：让用户自证要用**返回字符串的 IIFE**（`JSON.stringify({... , same})`），
  不要以 `console.log` 结尾——DevTools 会把 `undefined`（log 的返回值）回显出来，用户会误判成"没生效"。

### 第四轮：按用户规范重做为「三段式」登录页（工单 #110 的最终形态）

规范：tab 在顶；邮箱+密码＝中间（切凭证只换中间）；登录/或/通行密钥＝底部，**两 tab 共用登录按钮**。

- 实现：顶部 `role="tablist"` → **单个 `<form>`** 覆盖中+底 → 中间 `div.grid` 内两个
  `<fieldset>`（同格、非活动者 `invisible` **+ `disabled`**）→ 底部共享 `type="submit"` + 「或」+ 通行密钥。
- 要点：同一个 form 里隐藏的 `required` 输入会**静默阻止提交**，故非活动字段集必须 `disabled`
  （fieldset 的子孙被排除出约束校验/提交）。共用按钮决定了必须单 form（否则得用 `form="id"` 动态指向）。
- CDP 实测三态逐位相同：`topTabs 277/32`、`midInput 362/44`、`midFieldset 333/166`、`submit 519/44`、
  `passkey 619/44`、`card 181/519`；`tabs` 与 `:disabled` 状态正确对调。
- 探针自身两个坑（已修）：点击与测量要**分两次 round-trip**（React 事件后提交，同 evaluate 量到旧 DOM）；
  判禁用要用 `matches(':disabled')`（`input.disabled` 不反映被 fieldset 连带禁用）。
- 门禁 typecheck 0 / lint 0 / i18n 1997 / unit 954；部署 `.microfeed/deploy8.log` → verified。
  截图 `.scratch/microfeed-rbac/login-three-band-{account,credential}.png`。

### ⚠️ 外部改动 + 部署挂死（19:20–19:55 这段的两件事）

**1) 17:28–17:31 有一批不属于我会话的改动**（deploy8 17:20 之后、用户 19:23 消息之前）：
`AdminCredentials.ts`/`better-auth.ts`/`ajax/account/password.ts`/`rbac-handlers.ts`/
`AdminPasswordSetupApp.tsx`/`AccountSettingsApp.tsx`/`UsersApp/index.tsx`/i18n/`AdminLoginApp.tsx`。
用 `git diff HEAD` 读实质 = **密码策略换代**：`MIN_ADMIN_PASSWORD_LENGTH` 12 → 9、
新增 `validateAdminPassword`（长度边界 + 大小写混合或含数字）、better-auth 与改密端点改取常量、
登录页去掉通行密钥行与硬编码 `minLength`；i18n 1997→1993（-4 键）、单测 954→956（+2）。
⇒ 用户要的密码规则**已由那批改动实现**。我只收尾：过期注释、`maxLength` 接常量、删掉误加的空 import。

**2) 部署挂死 26 分钟**：链式命令里部署那段漏了 `mv dist` → 卡在 `Building the Worker`
（该阶段 10821 帧、`Checks and build passed` 从未出现）。**判据：spinner 持续写日志 ⇒ mtime 不能当进展证据**，
要按阶段标记统计帧数。处置：停任务 → 补 `mv dist` → 重跑 `deploy10.log`，**3m02s 成功**。
**部署命令里的 `mv dist` 预清不可省，且必须和 deploy 写在同一行命令里。**

**3) 线上核对**：新 chunk `AdminLoginApp.ByR3iqCV.js` 中 `passkeySignIn`=0 / `passkey`=0
（通行密钥行已下线），`tabAccount`/`tabCredential`/`col-start-1`/`preventScroll` 各 1（三段布局完好）。

**4) 未完成**：账号「邮箱或用户名」。查证 better-auth 核心自带 `username` 插件（含 `/sign-in/username`，
无需新依赖），但要给 `auth_user` 加 `username`/`displayUsername` 列（动此前红线）+ 改 `better-auth.ts`；
且 better-auth 的 email 必填 ⇒ 无邮箱账号需占位邮箱。方案已给用户，待拍板。

### 第五轮：密码统一 ≥6 上线 + 账号标识方案

- 用户拍板：密码最小长度 **统一 ≥6**；账号 = 邮箱或用户名，**两种都要能登录**。
- 密码就是一处常量：`MIN_ADMIN_PASSWORD_LENGTH` 9 → 6（注释同步）；落点全由它派生
  （better-auth `minPasswordLength`、改密/建号端点、三个表单的 `minLength`、CLI）；文档与 CLI 无写死长度；
  i18n 用 `{{min}}` 插值无需改文案。
- 旧断言改为**从常量推导边界**（`Ab1 + "x".repeat(MIN-4|MIN-3)`、`MAX+1`），避免下次再留过期断言。
- **教训（已写进技能）**：`cmd | tail -6` 的退出码是 **tail 的**，导致"单测失败却照样部署"。
  重跑改为每条 `> log 2>&1; echo "$?"`：typecheck/lint/i18n/unit/worker 全 exit=0，unit **956 passed**。
- 部署 `.microfeed/deploy11.log` → verified（≥6 已在生产）。
- 账号方案文档：`.scratch/microfeed-rbac/account-username-plan.md`（需动 `better-auth.ts` + `auth_user` 加两列
  + 迁移 0039 + 登录/建号 UI + 测试；待拍板 4 点）。

### 第六轮：账号「邮箱或用户名」实现并上线

- 用户回「按方案执行」→ 四点默认值照准（授权动两处红线 / 建号形态 A / 占位域名 `users.microfeed.local` /
  仅新建可设用户名）。
- 迁移 `0039_auth_user_username.sql`（`username` + `displayUsername` + 唯一索引）；`better-auth.ts` 注册官方
  `username` 插件；`auth-client.ts` 加 `usernameClient()`；登录页字段改「账号」，按**是否含 `@`** 分派
  `signIn.email`/`signIn.username`；建号端点自动判别 + 占位邮箱 + 重名预检；
  `createUser({data:{username,displayUsername}})` 由插件钩子做权威校验与规范化（一次调用完成）。
- 有意偏离方案两处：用户名字符集改用**插件默认** `/^[a-zA-Z0-9_.]+$/`（不含连字符）；不需"先建号再补用户名"。
- 查实：`auth.api.createUser` 带 headers 且无会话时抛 UNAUTHORIZED（worker 测试须先铸真会话）；
  插件落库 `username` 小写、`displayUsername` 保留原始大小写。
- 顺带修过期文案 `rbac.passwordHint`（硬编码 8 → `{{min}}`）。
- 测试 +8（worker 5 + unit 3）；门禁 typecheck 0 / lint 0 / i18n **1999** / unit **959** / worker **262**；
  部署 `deploy12.log` → verified。**生产核对**：`auth_user` 两列 + 唯一索引已落，线上 chunk 含
  `login.account`/`microfeed-login-account`，`passkey`=0。
- 教训（已写进技能）：astro check 输出带 **ANSI 颜色码**，`grep "error ts("` 搜不到，
  必须先 `sed 's/\x1b\[[0-9;]*m//g'` 剥色；`APIRoute` 返回 `Response | Promise<Response>`，
  测试桩写成 `Promise<Response>` 只有 `tsc --noEmit` 会报。

### 第五轮：登录页去掉通行密钥行 + 密码策略放宽（SSOT 收敛）

- 登录页删除「或 · 使用通行密钥登录」整行 + `signInWithPasskey()` + `passkeySubmitting` +
  5 个 i18n 键（`login.or` / `passkeySignIn` / `passkeyWaiting` / `passkeyFailed` / `passkeyUnavailable`）。
  **通行密钥本体仍在**（插件/表/账户页管理/step-up 未动），只是登录页没入口了 → 账户页那段是否也隐藏待确认。
- 密码策略：长度 **> 8** 且（同时含大小写 **或** 含数字）。SSOT = `src/shared/AdminCredentials.ts`
  （`MIN_ADMIN_PASSWORD_LENGTH` 12→9，`validateAdminPassword` 加组合规则）。一处改动覆盖
  setup 表单、bootstrap、`manage auth` CLI、admin 建号、账户页改密。散落硬编码全部收敛：
  `better-auth.ts` 的 min/max 引用常量、`rbac-handlers.ts` 的 `<8`、`ajax/account/password.ts` 的 `<12 || >128`
  都改走 `validateAdminPassword()`；三处表单 `minLength` 引用常量；**登录页移除 `minLength`**（登录不校验策略）。
- i18n：+`errors.password.policy`（`{{min}}`，三处共用）；`passwordSetup.passwordDescription` 改文案。
  计数 1997 → **1993**。
- 测试：+2 例边界（8 位太短 / 9 位通过；全小写·全大写被拒）。**坑**：旧夹具 `"too short"` 恰是 **9 字符**，
  新规则下不再太短 → 改 `"short"`。7 处 worker/CLI 的**设置类**夹具改首字母大写（登录类不动）。
- 门禁：typecheck 0 / lint 0 / i18n 1993 / unit **956** / worker **257**；部署 `deploy9.log`。
- **待拍板**：账号标识支持用户名 → 需 better-auth `username()` 插件 + **给 `auth_user` 加列**（新迁移、动结构、
  与 RBAC 红线冲突）+ 改 `better-auth.ts`；上游无此功能（plugins 仅 admin/passkey/oauthProvider）。

## 上游升级影响评估（20:44，直查上游 GitHub 实测）

- **username 方案触及 9 文件，3 个上游拥有**：`AdminCredentials.ts` / `better-auth.ts` / `auth-client.ts`，
  合计 **+100 / −6 行**（better-auth 仅 import 加一项 + plugins 加一项 + 两个常量引用）。其余 6 个最后修改者是
  本地身份或 `andylxy`，属"继续分叉"。
- **迁移同号已真实发生**：上游主干已发 `0023_multilingual_search.sql`，本地同号 `0023_ext_novel.sql`；
  上游止于 0022，本地 0023–0039（17 个）上游不认识。此前"将来才撞"的说法**已更正**。
- **同号不是硬故障**：全仓无代码解析编号前缀，`d1_migrations.name` 与快照清单 `{filename, sha256}` 都以
  文件名为键 ⇒ 只影响排序可读性，deploy/快照不会失败。
- 上游最后提交 2026-09-21 19:24（release 1.0.12）⇒ 仍活跃。
- 未提交规模：37 已改（+2295/−544）+ 49 未跟踪 ⇒ **合并前先提交干净**是当下最该做的事。

## 后台菜单与权限的关系（22:47 查证）

- **主菜单不按权限显示**：`AdminShell.astro:133` 调 `getAdminNavigationItems(adminPath, activeNavItem,
  onboardingResult)`（无权限参数），`AdminNavigation.ts:34` 全量 map 16 项，`disabled` 只看 onboarding。
- 权限链路完整：middleware:454 → `resolveRbacContext()`（resolve.ts:73）→ 三表 join 得权限码集合
  → `locals.rbacPermissions`（middleware:459；认证关闭时给 `*`，middleware:479）。
- **真边界在 ajax**（`requireRbac` 仅见于 rbac-handlers / webhook-handlers）；页面级守卫仅 2 处
  （users:19 = `system:user:manage`、rbac:20 = `system:role:manage`），其余 14 个目的地无守卫，
  被拒时是**裸文本 403**。
- **权限目录不覆盖菜单**：seed.ts:16 的 29 码里仅 9 个能对上菜单；PAGES/SITE_FILES/REVIEW/AUDIT/
  EDIT_CHANNEL/API 管理页 **6 项无对应码**（`api:*` 属签名调用语义）；ADMIN_HOME 特例。
- 隐蔽点：`guard.ts:110-117` 的 `*` 与 legacy admin（`role==="admin"`）都无条件放行 → 菜单过滤需同样处理；
  `AdminShell.astro` 从不把权限集传给组件（客户端只拿到 email）⇒ 浏览器不知道自己有哪些权限。
- 路线：A 服务端过滤（推荐，无闪烁、不下发权限模型）；B 客户端过滤（有闪烁，且与"防止仅登录就读到
  整个权限模型"的意图相悖）。
- **方案文档** `.scratch/microfeed-rbac/admin-menu-rbac-plan.md`（工单 #111–#118）：16 项菜单↔权限码
  对照、A 路线逐处改动、14 页守卫补齐清单。
- 关键发现：① **editor 无任何 `content:article:*`**，而 `ajax/feed.ts:148-153` 要求 article:update
  ⇒ editor 现在无法编辑/新建章节；按 article 码过滤菜单会让 editor 丢掉内容入口 ⇒ 补授权须先做。
  ② 5 个读码（book/category/volume/article 的 read、article:create）在目录里但**从未被端点使用**，
  只能当菜单可见性标记。③ 多数 `/ajax/*` 只有身份门没有权限门（items/pages/site-files/review/
  audit/themes/api-keys/r2-ops/volumes-index）。
- **菜单方案照 BasicApp 重做**（新文档 `.scratch/microfeed-rbac/admin-menu-basicapp-design.md`，
  取代旧的 `admin-menu-rbac-plan.md`）。参照实现在本机 `D:\git\AiCode\XiHan.BasicApp`：
  菜单是**数据表行**（Sys_Menu），行上带**可空外键 PermissionId** 绑一个权限点（单一、不做多对多；
  需多权限就建组合权限点），各模块用 PageRegistry 声明式登记、Seeder 按 MenuCode 幂等落库（权限缺失则
  跳过并告警），加载 = 过滤→建树→剪空目录→**兜底**（永不空菜单），按权限集缓存。
  **我上一版错在把映射写进代码**。microfeed 适配：新表 `ext_menu` + `permission_code`（用 code 不用 id，
  因 locals.rbacPermissions 本就是 code 集合）；不缓存、不做多租户/外链/按钮级。editor 授权缺口结论不变。
- **访谈定稿 10 项**（grill-with-docs）：本期就建 6 个新权限码并全绑菜单（否则菜单可见 vs 页面守卫打架）；
  页面守卫须与菜单同码；editor 只拿内容域（article r/c/u + page + site_file，11→16）；不给 article:delete；
  二级与按钮级不做；**表是唯一真相**（删 NAVIGATION_PATHS）；子侧栏不并入；**命名全统一为 menu**
  （约 40 处引用 + 32 个 i18n 键 + check-i18n.ts:94）；不做菜单管理 UI。
- 产出：`D:\git\AiCode\simple-rbac-design\ADR-002-admin-menu-as-data.md` + GLOSSARY.md 新增菜单词条；
  microfeed 侧 `.scratch/microfeed-rbac/admin-menu-basicapp-design.md` 已更新为定稿。迁移 0040（码+授权）、
  0041（建表+播种）。editor 可见 8 项 / 不可见 8 项。
